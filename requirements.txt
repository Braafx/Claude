# The newsletter uses only Python stdlib — no external packages required.
# If you add features (e.g. Unsplash API client, Markdown rendering), add deps here.
# Install with: pip install -r requirements.txt  OR  uv pip install -r requirements.txt
